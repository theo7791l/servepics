# Server Configuration
PORT=3000
NODE_ENV=production

# Session Security
# ⚠️ CRITICAL: Generate a secure random string (minimum 32 characters)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS

# Default Admin Account
# 🔒 IMPORTANT: Change these credentials immediately after first login!
ADMIN_EMAIL=admin@servepics.com
ADMIN_PASSWORD=ChangeThisPassword123!

# Security Recommendations:
# 1. Never commit your .env file to git
# 2. Use different secrets for dev/prod environments  
# 3. Rotate SESSION_SECRET periodically
# 4. Use strong passwords (min 8 chars, mixed case, numbers, symbols)
# 5. Change default admin credentials on first login
